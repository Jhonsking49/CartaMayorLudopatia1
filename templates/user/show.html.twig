{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr> #}
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Saldo</th>
                <td>{{ user.saldo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_main') }}">Vuelve al inicio</a>

    <a href="{{ path('app_user_edit', {'id': user.id}) }}">Añádete saldo</a>

    {# {{ include('user/_delete_form.html.twig') }} #}
{% endblock %}
