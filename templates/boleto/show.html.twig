{% extends 'base.html.twig' %}

{% block title %}Boleto{% endblock %}

{% block body %}
    <h1>Boleto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ boleto.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ boleto.numero }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_boleto_index') }}">back to list</a>

    <a href="{{ path('app_boleto_edit', {'id': boleto.id}) }}">edit</a>

    {{ include('boleto/_delete_form.html.twig') }}
{% endblock %}
