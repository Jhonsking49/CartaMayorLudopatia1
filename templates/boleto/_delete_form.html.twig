<form method="post" action="{{ path('app_boleto_delete', {'id': boleto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boleto.id) }}">
    <button class="btn">Delete</button>
</form>
