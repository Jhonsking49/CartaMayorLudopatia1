{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Ludopatía 🤺(tu familia)</h1>

    {% if is_granted('ROLE_ADMIN') %}
        <a href='/sorteo/new'>Crea un sorteo</a><br>
    {% endif %}
        <a href='/sorteo'>Revisa los sorteos</a><br>
    {% if is_granted('ROLE_USER') %}
        <a href="{{ path('app_user_show', {'id': app.user.id}) }}">Organiza tu perfil</a><br><br>

        <h3>Sorteos finalizados</h3>
        {% for sorteo in sorteos %}
        <a href="{{ path('app_sorteo_show', {'id': sorteo.id}) }}">{{sorteo.nombre}}</a><br>
        {% endfor %}
    {% endif %}

</div>
{% endblock %}
